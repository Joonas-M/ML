(ns ml.core-test
  (:require [clojure.test :refer :all]
            [ml.core :refer :all]
            [clojure.data.csv :as csv]
            [clojure.java.io :as io]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
